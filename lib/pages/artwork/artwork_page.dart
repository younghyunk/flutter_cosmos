import 'package:flutter/material.dart';

class ArtWorkPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('art work page'),
    );
  }
}
