import 'package:flutter/material.dart';

class MonoPhotoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('mono photo page'),
    );
  }
}
